<Window x:Class="ChessGame.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChessGame"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Buttons row -->
            <RowDefinition Height="*"/>
            <!-- Chess board -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Buttons -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0" Height="30" VerticalAlignment="Top">
            <Button Content="New Game" Click="NewGame_Click" Width="100" Margin="5"/>
            <Button Content="Save Game" Click="SaveGame_Click" Width="100" Margin="5"/>
            <Button Content="Load Game" Click="LoadGame_Click" Width="100" Margin="5"/>
        </StackPanel>

        <!-- Chess Board -->
        <local:ChessControl x:Name="ChessControl" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Height="590" Width="590"/>

        <TextBlock x:Name="TurnLabel" Grid.Row="2" Text="Turn: White" 
                   HorizontalAlignment="Center" VerticalAlignment="Center" 
                   FontSize="16" FontWeight="Bold" Margin="0,10,0,10"/>
    </Grid>
</Window>
